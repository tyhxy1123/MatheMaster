\tikzset
{
	treenode/.style = {circle, draw=black!60, fill=white!40, very thick, minimum size=6mm}
}

\begin{figure}[H]
	\center

	\begin{tikzpicture}%[level/.style={sibling distance = 2cm/#1, level distance = 3cm}]
	
	\def \xone{0};
	\def \yone{0};
	
	\coordinate (root) at (\xone ,\yone);

	\draw (root) ++ (-1,0) -- ++(2,0);
	\draw (root) ++ (0,-1) -- ++(0,2);
	\draw (root) ++ (-1,-1) -- ++(2,2);
	\draw (root) ++ (1,-1) -- ++(-2,2);

	\node [treenode] at (root) {-20};
	\node [treenode] at (root) ++ (-1,0) {1};
	\node [treenode] at (root) ++ (1,0) {1};
	\node [treenode] at (root) ++ (0,1) {1};
	\node [treenode] at (root) ++ (0,-1) {1};

	%diagonal nodes
	\node [treenode] at (root) ++ (-1,-1) {1};
	\node [treenode] at (root) ++ (-1,1) {1};
	\node [treenode] at (root) ++ (1,1) {1};
	\node [treenode] at (root) ++ (1,-1) {1};

		
	\end{tikzpicture}
	\caption{9-point stencil (second order accurate)(6th order accurate for harmonic functions)}
\label{nine_point}
	
\end{figure}
